CREATE TABLE "CRYPTO" (
	"ABR"	TEXT NOT NULL UNIQUE,
	"name"	TEXT NOT NULL UNIQUE,
	"img"	BLOB NOT NULL,
	PRIMARY KEY("ABR")
);


CREATE TABLE "CRYPTO" (
	"ABR"	TEXT NOT NULL UNIQUE,
	"name"	TEXT NOT NULL UNIQUE,
	"img"	BLOB,
	PRIMARY KEY("ABR")
);