CREATE TABLE "PORTFOLIO" (
	"ID_portfolio"	INTEGER NOT NULL UNIQUE,
	"ID_user"	INTEGER NOT NULL UNIQUE,
	FOREIGN KEY("ID_user") REFERENCES "USER"("id_user"),
	PRIMARY KEY("ID_portfolio" AUTOINCREMENT)
);